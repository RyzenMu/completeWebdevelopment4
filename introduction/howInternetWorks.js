/**
 * 
 * the computer which data stored 24/7 is called server
 * client - the computer which access servers data
 * 
 * step 1 - your browser sends request to ISP (Internet Service provider).
 * step 2 - ISP sends to DNS (Domain Name System) server.it is like a phone book contains web address to corresponding IP address.
 * step 3 - once DNS locates the unique IP address, it sends the response back to your browser.
 * 
 * NsLookup.io -- gives the ip address of web pages.
 */