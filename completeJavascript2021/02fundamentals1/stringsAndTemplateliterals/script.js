const firstname = 'jonas';
const job = 'teacher';
const birthYear = 1991;

const fullname = `I\'m ${firstname} -- i work as a ${job} -- my age is ${2024-1991}.`;
console.log(fullname);

// multi line string are used using back ticks (`template literals`)

console.log(`
hi 
i'am
${firstname}
i works as a
${job}`);